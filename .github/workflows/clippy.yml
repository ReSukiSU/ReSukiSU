name: Clippy check

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/clippy.yml'
      - 'userspace/**'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/clippy.yml'
      - 'userspace/**'

env:
  RUSTFLAGS: '-Dwarnings'

jobs:
  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: rustup update stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: userspace/ksud

      - name: Setup cargo-ndk
        run: cargo install cargo-ndk --locked

      - name: Run Clippy
        run: |
          cargo ndk -t arm64-v8a --manifest-path userspace/ksud/Cargo.toml clippy
          cargo ndk -t arm64-v8a --manifest-path userspace/ksuinit/Cargo.toml clippy
          cargo ndk -t x86_64 --manifest-path userspace/ksud/Cargo.toml clippy
          cargo ndk -t x86_64 --manifest-path userspace/ksuinit/Cargo.toml clippy
