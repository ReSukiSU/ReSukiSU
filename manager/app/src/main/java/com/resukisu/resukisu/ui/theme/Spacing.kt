package com.resukisu.resukisu.ui.theme

import androidx.compose.ui.unit.dp

/**
 * The single horizontal gutter between screen content and the display edge.
 *
 * Screen-level lists, cards and the top app bar pills all align to this. Component-internal
 * padding (text inside a card, a button's own content padding) is deliberately not this value.
 */
val ScreenEdgePadding = 16.dp
